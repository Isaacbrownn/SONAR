version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto17  # Install Amazon Corretto 17 (Java 17)
    commands:
      - java -version  # Check installed Java version
      - wget https://binaries.sonarsource.com/Distribution/sonar-scanner-cli/sonar-scanner-cli-3.3.0.1492-linux.zip
      - unzip sonar-scanner-cli-3.3.0.1492-linux.zip
      - export PATH=$PATH:./sonar-scanner-3.3.0.1492-linux/bin/

  pre_build:
    commands:
      - sonar-scanner -Dsonar.organization=Isaacbrownn -Dsonar.projectKey=Isaacbrownn_SONAR -Dsonar.sources=src -Dsonar.host.url=https://sonarcloud.io -Dsonar.login=072cb06e466b818bbe1555ef40579f0b05672b04

  build:
    commands:
      - echo Build completed

cache:
  paths:
    - sonar-scanner-3.3.0.1492-linux/
